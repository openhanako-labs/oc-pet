# oc-pet × 开源桌宠/Agent 框架 对比与规划

> 作者：奥菲莉娅（Ophelia）
> 日期：2026-09-19
> 来源：oc-pet 源码（ARCHITECTURE.md + 实测 memory_hybrid/memory_embedding）+ 开源分析资料（02-桌宠VTuber、07-Cortico×AgentAtelierR、AAAAGENT/Amadeus/AmadesuEngine README 原文）
> 定位：横向比对「我们缺什么 / 能优化什么 / 该新增什么」

---

## 0. 一句话结论

oc-pet 在**表达层、感知层、生命周期层**已经比绝大多数开源桌宠成熟（渲染/口型/TTS/ASR/屏幕感知/多宠/主动对话/MCP 提供端全齐）。它的本质是 **Hana 的翻译层（嘴+眼+表演者），不是脑**——LLM 推理、工具选择、多轮上下文、长期记忆主存储全在 Hana 服务端。

所以对比的「缺口」不是「功能缺」，而是**三个结构性断层**：
1. **脑在别处**：独立 Agent 循环 / 多模态视觉进对话 / subagent / 具身 全部未落地。
2. **渲染只到 2D**：VRM/3D 是占位，物理反馈为零。
3. **生态本地化浅**：本地 plugins/ 只有 1 个，MCP 提供端默认关，per-agent vision 未配导致 429。

而开源项目的真正参考价值，集中在三点：**表演标签协议（AgentAtelierR）、事件流批处理（Cortico）、Host 三分架构的权限持久化（Amadeus）**——其余要么是重复造轮，要么是 PPT 级。

---

## 1. 比对矩阵（已实现 × 开源标尺）

| 能力维度 | oc-pet 现状 | 开源对照标尺 | 差距判定 |
|---|---|---|---|
| **定位** | Hana 翻译层（LLM/工具/记忆全借道） | FaustBot/Open-LLM-VTuber = 独立 Agent；Amadeus = Agent Host | oc-pet 设计选型不同，不是落后 |
| **渲染 2D Live2D** | ✅ 完整（Cubism 2/3/4，>2700 行） | AAAAGENT（需本地适配）、AmadesuEngine（Unity VRM 3D） | 平手，oc-pet 更扎实 |
| **渲染 3D/VRM** | ❌ 占位（`vrm_renderer.load()` 恒返 False） | AmadesuEngine（Unity VRM 真实渲染）、Mate-Engine（VRM 3D） | 🔴 真实缺口 |
| **口型同步** | ✅ 三级回退（音素/词级/能量） | AgentAtelierR 有精确参数（20ms帧/40ms起音/90ms收音/≤55%/闭嘴≥60ms） | 🟡 oc-pet 参数靠经验，可对齐 |
| **TTS** | ✅ 5 引擎（Edge/CosyVoice2/MiMo/Qwen3/API） | Amadeus（GPT-SoVITS v3 + Fish Audio）、AmadesuEngine（mimo/indextts） | 平手，oc-pet 引擎更多 |
| **ASR** | ✅ 3 引擎 + Silero VAD + 声纹 | Amadeus（Qwen3-ASR/SenseVoice）、AmadesuEngine（OpenAI 兼容） | 平手 |
| **记忆召回** | ✅ BM25+cosine(接口)+RRF；**无 embedding 时退化 BM25-only** | FaustBot（SQLite真源+graph+networkx）、Open-LLM-VTuber（外包 Letta）、Cortico（memoTiers/subconscious） | 🟡 缺图关系层 + 分层记忆 |
| **对话 Agent 循环** | ❌ 无（编排器，推理在 Hana） | FaustBot（LangGraph）、Amadeus（Talk 闭环）、Cortico（事件流 loop） | 设计选型，但 direct 模式能力残缺 |
| **多模态视觉进对话** | ⚠ 屏幕只判情绪/活动，不入 image 通道 | Amadeus（visual-context 链，但图像仍随主 Chat） | 🟡 同源痛点，均未独立 vision 路由 |
| **主动对话调度** | ✅ 完整（ProactiveScheduler 967 行） | AAAAGENT（有限情绪分类） | oc-pet 更强 |
| **工具路由 / 插件** | ✅ 三层（本地能力/Hana目录/Node插件），本地 plugins 仅 1 个 | Amadeus（Provider 委派，MCP/Skills 不暴露给 Main Chat） | 🟡 本地插件生态浅 |
| **MCP 提供端** | ✅ 9 工具（pet_*），默认关，无 Origin 校验/超时 | AmadesuEngine（5 工具，实时开关+Origin校验+8s超时） | 🟡 oc-pet 实现可补强 |
| **A2A / subagent** | ❌ 未实现 | Amadeus（Work Provider 委派）、AAAAGENT（任务卡转交 Codex） | 🔴 红差距 |
| **具身（Embody）** | ❌ 未集成（仅情绪→参数映射抽象） | Amadeus（语音/字幕/口型/表情同帧时间线） | 🔴 真实断层 |
| **表演标签协议** | ✅ `[feel:]/[emotion:]/[do:]` 文本标签 | AgentAtelierR（face 9 类 + action 12 类 + Fish 情绪/表演，模型只能从目录选） | 🟡 oc-pet 用 [do:] 让模型猜动作名，有幻觉风险 |
| **权限/恢复持久化** | ⚠ 重启靠 Hana 会话 | Amadeus（Host 管身份/权限/Artifact/重启恢复） | 🟡 可借鉴 Host 层 |
| **游戏语境理解（陪玩）** | 🔴 三大红差距（画面深度识别/场景文案/A2A 副官） | 无对照（开源桌宠不做陪玩） | 🔴 oc-pet 独有方向 |
| **Web 管理界面** | ❌ 无（仅托盘/右键菜单/面板） | AAAAGENT（网页管理角色/Prompt/记忆/召回） | 🟢 可新增 |
| **跨端（微信/移动）** | ✅ 手机感知（MacroDroid/linjian-peek） | AAAAGENT（微信延续对话） | 不同路线，可参考 |

---

## 2. 缺口清单（GAP — 结构性断层）

### G1 🔴 VRM / 3D 渲染（占位）
- `avatar/vrm_renderer.py` 全文件 `load()` 返回 False；`factory.py` 三处「未来 3D 扩展钩子」。
- 开源对照：AmadesuEngine（Unity VRM 真实渲染）、Mate-Engine（VRM 3D）。
- 决策：自研 3D 成本高、ROI 低（分类总览已否决 Blender 路线）。**建议：短期不碰真实 3D，只补 VRM 加载器骨架 + 物理反馈占位接口**，等明确需求再投入。

### G2 🔴 A2A / subagent 调用（红差距）
- `docs/playmate-gap-analysis.md` 标红：无通用 Agent 调用（Hermes/OpenClaw 类）。
- 开源对照：Amadeus（Work Provider 委派执行）、AAAAGENT（任务卡转交 Codex）。
- 决策：**这是「桌宠自主干活」野心的核心**。可借 Amadeus 的「角色叙述 / Provider 执行 / Host 权限」三分，让桌宠把活委派给 Hana agents（已有 7 个），而非自研 agent 运行时。

### G3 🔴 具身（Embody）未集成
- 只有 `emotion→参数` 抽象，无「语音/字幕/口型/表情沿同一条播放时间线」。
- 开源对照：Amadeus Embody 层（同帧时间线）。
- 决策：oc-pet 已有 lip_sync + [feel:]/[do:] 时间轴，缺的是**一个统一的表演时间线调度器**把口型/表情/动作绑到 TTS 播放进度。可借鉴 AgentAtelierR 的表演队列（去重/限长/冷却）。
- **⚠️ 2026-09-19 复核：本节不成立。** 所谓「缺统一表演时间线调度器」是错的——`avatar/motion_mixer.py` 就是分层仲裁器（`Layer` 优先级 + `MotionRequest` + 3s 防闪烁 + 过渡缓动 + duration 过期），配 `_expire_intent_params` / `_expire_expression_if_stale` / 口型时间线，**五轨齐备**。

### G4 🟡 记忆只到 BM25+向量融合，缺图关系 + 分层 + 检索加权（源码级证据：深度分析/01-ocpet-memory-vs-faustbot.md）
- **实测架构**：`memory_facts`(JSON) + `memory_reflection`(JSON) + `memory_hybrid`(BM25+cosine+RRF 纯算法模块) + `memory_embedding`(本地 ONNX) + `memory_filter`(隐私过滤)。**无图、无持久化向量索引、无检索加权**。
- **2026-09-19 复核更正**：
  - **「无分层」不成立**。分层是**跨模块实现**而非单一 JSON 的 tier 字段：`CompanionMemory`＝短期/日常层（7 天滚动 `HISTORY_KEEP_DAYS=7`、话题/事件/日摘要/连续天数）；`FactStore`＝长期/语义层（`status` 状态机 pending→confirmed→promoted/archive_candidate + 证据双时钟衰减 + `protected` 永不裁剪 + `importance`/`confidence`）；另有 `SceneMemory`（场景）、`memory_reflection`（反思，带 `by_confidence`）。原判源于「只看了某一个 JSON 的 schema」。
  - **「无检索加权」已补**（G4①，2026-09-19）：`score_patch` 钩子 + `make_salience_patch`。
  - **仍真实缺失**：① 实体图（facts 虽有 `subject/predicate/object` 字段，但无图结构/遍历）；② 持久化向量索引（每次 recall 现算）。
- `memory_hybrid.py`：cosine 路径受 `memory.embedding.enabled` 门控，默认 false → **多数用户跑 BM25-only**（确认初版判断）。
- 开源对照：FaustBot（SQLite nodes/edges + networkx + 双 NanoVectorDB + BM25 + rerank + 图）、Cortico（memoTiers/subconscious）。
- **值得借（P0）**：① `score_patch`（FaustBot 的 `[-0.15,0.15]` 单节点检索加权；oc-pet 当前 `memory_filter` 只有二值判断「是否进调度」，无法表达手动加权/降权）；② **持久化向量索引**（FaustBot 的 `chunks.vdb` 思路——当前每次 `recall` 都对 pool 重算 embedding，延迟随规模线性增长）；③ BM25 索引懒加载 + dirty 标记（P1）。
- **明确不引入**（深度分析01 判据）：SQLite 真源（JSON 在 `FACT_MAX_FACTS=2000` 规模够用）、networkx 图（记忆模型是「平铺+时间+证据强度」非图）、`_CrossLoopLock`（oc-pet 是 threading 架构、无 asyncio，不适用）、外部 rerank API（隐私+延迟冲突）、实体-关系抽取（短期无需求）。
- **oc-pet 已有强项（对比 FaustBot 不弱）**：零向量库依赖、fallback gate 贯穿全层、繁简折叠对称、CJK 2/3-gram、证据双时钟衰减（reinforcement 30天 / disputation 14天 + sigmoid confidence，比 FaustBot 静态 score_patch 更细）、隐私过滤（情感类不进调度避免恐怖谷）。AAAAGENT 召回公式 `P=C×(0.55+0.25A+0.15I+0.05E)` 可作 score_patch 情绪权重参考。

### G5 🟡 多模态视觉不进对话 + 屏幕感知占 73% LLM 时间（源码级证据：深度分析/04-屏幕感知与WS缺口.md）
- **73% 时间三根源**（同一架构债的三种症状）：① 一次屏幕变化 = 两次 LLM 调用（vision API `screen.py:660-680` + enrich LLM `screen.py:379-410`）；② 三消费者并发共用同一 provider（vision + enrich + proactive，无全局并发信号量）；③ MD5 整帧缓存颗粒太粗（光标/进度条/时间戳任一变化即换 hash，无相似帧检测）。
- 429 限流：harness_adapter 只处理 Timeout/ConnectionError，429 走 HTTPError 直接返回兜底、无退避；视觉 API 与对话共用 `models.chat`（vision 未配时 fallback）。
- WS 无 context 通道：`hanako_ws_client.send_prompt` 白名单只有 `text/displayMessage/uiContext/sessionFileRefs`，`extra_context` 被迫拼进 text → 污染会话标题/历史（`harness_adapter.py:809`）。
- 开源对照：Amadeus visual-context 链（图像仍随主 Chat，未独立 vision 路由）——**同源痛点，均未独立 vision 路由**。
- 决策：已支持 `models.vision`（2026-09-17 新增）+ `_UTILITY_SOURCES` 走 utility_model（已做的分离）；缺口在「用户未配置」+「无全局 LlmRouter 并发控制」+「无 pHash 相似帧检测」。

### G6 ✅（已更正，非缺口）表演标签已有白名单约束
- **2026-09-19 代码核实更正**：原判「模型自由猜动作名」不成立——`_AI_DO_ALIASES`（语义标签→预设/motion）+ `_AI_DO_PROMPT`（注入 prompt 白名单）+ `_build_action_prompt()`（「只能从这些里选」）+ 未命中 `return False` 安全回退 **均已存在**。
- 残余（低 ROI）：白名单是**静态**的（不随角色实际 motion/表情生成）；且 `[do:]` 全历史 0 次使用。
- 决策：**降级**。如做，只需把 `available_actions`（pet.json，动态）接进 `_build_action_prompt`。

### G7 🟡 本地插件生态浅
- `plugins/` 仅 1 个（linjian-peek 8 工具）；外部插件在 `~/.hanako/plugins/`。
- 开源对照：Amadeus Provider 生态、netease-music-mcp（18 工具）。
- 决策：把 Hana 侧已有 92 skills / 26 plugins 的目录，更主动地暴露成桌宠本地可调用的能力（当前只入目录不主动调）。
- **⚠️ 2026-09-19 复核：「机制缺失」不成立。** 插件机制完备：`tool_registry` + `conversation_engine._plugin_roots()` 扫**两个根**（`~/.hanako/plugins` + 本地 `plugins/`）、manifest 目录戳**热重载**、工具进 LLM 工具表、`ui/plugin_panel.py` 面板、`PetPermissions.tool_call_enabled` 闸门。本地只装 1 个是**数量**，不是机制缺失——真实议题仅是「生态浅」。

---

## 3. 优化清单（OPT — 已有基础，补强）

| 编号 | 项 | 现状 | 优化方向 | 开源借鉴 |
|---|---|---|---|---|
| O1 | 屏幕感知 429 限流 + 73% LLM 时间 | 三消费者并发（**P0 429 退避已落地 + P1 dHash 近邻去重已落地**） | **P0**：视觉 API 429 立即 backoff（改 `screen.py:796`）+ 日志埋点 `(source,provider,model,status,elapsed)`；**P1**：MD5→pHash 相似度 + Proactive 加 busy_check + 强制配置 `models.vision`/`utility_model` 独立 provider；**P2**：全局 `LlmRouter` 并发信号量 + enrich 硬预算 + `[pet-context]` 无内容不拼块 | Cortico batching + 深度分析04 方案集 |
| O2 | MCP 提供端安全性 | **已核实（2026-09-19 更正）**：9 工具 + 只绑 127.0.0.1 + 默认关 + `auth_token` + `allow_actions` 只读 + 动作白名单（原「零安全」不成立） | 残余仅 Origin 校验/超时（边角，服务器默认关） | AmadesuEngine McpPetServer |
| O3 | 口型参数与时序 | LIP-1 三级回退（前向预测）经验值 | 对齐 AgentAtelierR 20ms/40ms/90ms/≤55%/闭嘴≥60ms；加 Amadeus `first_mouth_minimum=0.12`（首帧强制最低开口度，零成本）；验证 mouth 采样发生在 PCM 写入前（深度分析02 时序约束） | AgentAtelierR + Amadeus |
| O4 | 记忆 embedding 默认关 | **已落地（2026-09-19）**：改用 API embedding（siliconflow `BAAI/bge-m3`，1024 维，密钥从 provider-catalog 动态解析、不入库） | ~~本地 jina-nano~~（ORT 版本不接受该 int8 模型，已删） | — |
| O5 | pet-context 只能进 text | WS 无 system/context 通道 | 把屏幕观察作为独立 World 事件，非塞 text | Cortico World 接口 |
| O6 | per-agent vision 未配 | **已复核（2026-09-19）**：视觉配置链**早已存在**且有测试（`env_config.py:401+` 优先级 preferences→.env→agent `models.vision`→catalog；`tests/test_vision_config_chain.py`） | 非代码缺口——只需用户在 Hana 设置页选一个视觉模型 | oc-pet 已支持，待配 |
| O7 | 表演队列 | **已复核（2026-09-19）**：`avatar/motion_mixer.py` 已是分层仲裁器（`Layer` 优先级 + `MotionRequest` + 3s 防闪烁冷却 + 同层 can_interrupt 规则），注释明写「替换 5 道互斥补丁」 | ~~加去重/限长/冷却的表演时间线~~ → **已存在**；可选增强：把口型也纳入层（但口型跟音频时钟，合并未必好） | AgentAtelierR 队列 |
| O8 | 技术债 pet.py 6 个 _init_* 未搬 | 搬=拆窗口构建 | 抽接口后再搬（设计改动） | — |
| O9 | ishiki.md 缺失 | 启动告警，LLM 缺行为约束 | 建 `~/.hanako/agents/miku/ishiki.md` | — |
| O10 | FrameBaker MCP 集成 | **已核实（2026-09-19）**：`FrameBakerMCP` 全库零调用 + 本机无 FrameBaker 实例（无 env/配置/目录）→ **死代码，非缺口** | 不实现；已改为 `NotImplementedError` 诚实报错 | — |

---

## 4. 新增清单（ADD — 开源有、我们无，且值得做）

### A1 🟢 Web 管理界面（角色/Prompt/记忆/召回可视化）
- 开源对照：AAAAGENT 网页管理（查看修改角色 Prompt、记忆、召回记录、上下文、语音、表现预设、连接状态）。
- 价值：oc-pet 当前只有托盘/右键/面板，调试记忆召回不直观。
- 成本：中（复用现有 `status_http_server` 8977 + 一个轻前端）。

### A2 🟢 能力目录快照（动作/表情白名单约束）
- 开源对照：AgentAtelierR `motionGroups` 快照 + face 9 类 + action 12 类。
- 价值：堵 `[do:]` 模型猜动作名的幻觉风险；统一表情/动作语义。
- 成本：低（加一张白名单表 + 快照生成）。

### A3 🟢 表演时间线调度器（Embody 轻量版）
- 开源对照：Amadeus Embody 同帧时间线。
- 价值：把口型/表情/动作绑到 TTS 播放进度，提升「在场感」。
- 成本：中（在 `conversation_engine` 的 on_reply 阶段加一层调度）。

### A4 🟢 A2A 委派骨架（借 Hana agents）
- 开源对照：Amadeus Work Provider 委派、AAAAGENT 任务卡转交。
- 价值：让桌宠「把活派出去」，是自主 agent 野心的落点。
- 成本：高（需定义委托协议 + 权限边界 + 结果回灌），建议作为 v2 主线。

### A5 🟢 Host 权限/恢复持久化层（轻量）
- 开源对照：Amadeus Host 三分（角色/Provider/Host）。
- 价值：重启恢复关键状态（当前靠 Hana 会话 ticket）。
- 成本：低-中（抽一层 `host_state` 持久化 identity/permission/artifact）。
- **⚠️ 2026-09-19 复核：权限/持久化两半均已存在。** 权限层：`perception/flags.py` `PetPermissions`（screenshot/diary/session_read/cross_session/tool_call + active_hours），`perception/controller.py` 逐模块强制。持久化分层：`~/.oc-pet/`（memory/usage/backups）+ `~/.hanako/`（agents/plugins/apps/provider-catalog），另有 `core/backup_service.py` **备份服务**。仅「artifact」作为命名概念未单列。

### A6 🟢 微信/跨端延续对话（可选）
- 开源对照：AAAAGENT 微信延续对话。
- 价值：脱离桌面也能接话。
- 成本：中（需微信桥，合规风险待评）。

---

## 5. 不建议做的（PPT 级 / 重复造轮 / 边界外）

- ❌ 自研完整 3D 渲染引擎（ROI 低，AmadesuEngine 已用 Unity 路线）；
- ❌ 抄 FaustBot 的 GraphStore 存储子系统（重，只抄检索不抄存储必炸）；
- ❌ 直接引用 AgentAtelierR 的 Dart 代码（无 license，仅可参考设计）；
- ❌ 深度集成 Cortico（5 天新仓 v0.1.1 pre-release，接口未稳，等 v0.2）；
- ❌ 把 Amadeus 部署为 Android 应用（桌面场景不同栈）；
- ❌ 自研独立 LLM 推理循环（oc-pet 的 Hana 翻译层选型正确，direct 模式仅兜底）。

---

## 6. 落地优先级建议（按 ROI 排序）

| 优先级 | 项 | 类型 | ROI |
|---|---|---|---|
| P0 | O4 开 embedding 默认 + O9 ishiki.md | 优化/补缺 | 高（低成本，直接提升记忆与角色约束） |
| P0 | O1 屏幕感知批处理（Cortico batching） | 优化 | 高（解 429 限流，体验直接改善） |
| P1 | A2 能力目录快照 + O6 表演队列 | 新增/优化 | 高（堵幻觉，提升演出质量） |
| P1 | O2 MCP 提供端安全补强 | 优化 | 中（默认关，影响小但成本低） |
| P1 | O5/O6 视觉进对话 + per-agent vision 配 | 优化 | 中（需用户配模型） |
| P2 | A1 Web 管理界面 | 新增 | 中（调试体验） |
| P2 | A3 表演时间线调度器 | 新增 | 中（在场感） |
| P2 | G4 记忆补强（score_patch + 持久化向量索引，不引入 sqlite/nx 图） | 缺口补强 | 中（检索加权 + 避免 recall 随规模线性变慢） |
| P3 | A4 A2A 委派骨架（v2 主线） | 新增 | 高但重（自主 agent 野心落点） |
| P3 | A5 Host 持久化层 | 新增 | 低-中 |
| 暂搁 | G3 全量具身、A6 微信 | — | 待明确需求 |

---

## 7. 关键判断（写进日记的锚点）

1. **oc-pet 不是「功能缺」，是「脑在 Hana、手在本地」的分工选型**——对比开源独立 Agent 桌宠时，不能用「它有没有 LLM 循环」判优劣，要看翻译层做得扎不扎实。结论：扎实。
2. **记忆缺口的精确表述**：不是「记忆弱」，是「只到 BM25+向量融合，缺图关系层 + 分层记忆」。开 embedding 默认即可补一半。
3. **开源真正可抄的三件事**：表演标签协议（AgentAtelierR）、事件流批处理（Cortico）、Host 权限持久化（Amadeus）。其余要么重复造轮，要么 PPT 级。
4. **A2A 委派是「桌宠自主赚钱/干活」野心的结构性落点**——但应借 Hana 已有 7 个 agents 做委派，不自研 agent 运行时。

## 8. 证据链与覆盖清单（定稿附）

### 8.1 深度分析专项（已并入正文第 2–3 章，附源码锚点）
- `深度分析/01-ocpet-memory-vs-faustbot.md` → **G4** 记忆层源码级结论（memory_* 六文件逐行 + FaustBot 逐项比对，给出 P0 借纳点：score_patch + 持久化向量索引）
- `深度分析/04-屏幕感知与WS缺口.md` → **G5 / O1** 屏幕感知 73% 三根源 + 429 无退避 + WS 无 context 缺口（含 `screen.py` / `harness_adapter.py` 行号，P0–P3 方案集）
- `深度分析/02-dsh-amadeus-下钻.md`（红莉栖）→ **O3** 口型时序（Amadeus 后向驱动 vs oc-pet LIP-1 前向预测）+ 第 5 章 dsh 3D 路径否决（15–25 人日 vs Live2D/VRM 3–5 人日）
- `深度分析/03-imapflow-迁移评估.md` → 邮件主线，与桌宠无关，不纳入

### 8.2 桌宠 / VTuber 项目覆盖清单
- **已源码/文档级对标（9）**：FaustBot / Open-LLM-VTuber / Amadeus / AAAAGENT / AmadesuEngine / Mate-Engine / AgentAtelierR / Cortico / dsh-blender-plugin
- **提到未展开（1）**：MewCo-AI（枫云助手 Linux 版，跨 Live2D/MMD/VRM 三形态切换；Linux 专属、未 clone 源码，仅架构参考，不构成代码级缺口）
- **不属桌宠主线（略）**：imapflow / Bulwark / Stalwart（邮件）、Spherse（实为 Agent 运行时，已剔除）、TTS/ASR 类（用户暂不管）

### 8.3 License 红线提醒（开源 subagent 横向观察）
- 桌宠主线几乎被许可红线切成两半：AGPL-3.0（Amadeus / folia-major / Bulwark / Stalwart）、非商业+署名（AAAAGENT / AmadesuEngine / Mate-Engine）、无 license（AgentAtelierR）。
- **反向结论**：oc-pet 自研 + Hana 桥路线不受外部 license 传染，在「能否商用」上反而最干净——这是选型隐含优势，非仅成本问题。
- 可集成（MIT/Apache）：livekit/agents（语音管道底座，原生 MCP）、netease-music-mcp、FaustBot（GPLv3）、Open-LLM-VTuber、Cortico（MIT，但 v0.1.1 未稳观察至 v0.2）。

### 8.4 晚到 subagent 增量（已并入正文，保留备查）
- 行业级佐证：「桌宠退化为角色外壳 + MCP 消费端」与初版「Hana 翻译层」判断一致，从 13 仓得到佐证。
- `first_mouth_minimum=0.12` → 已进 O3；AAAAGENT 召回公式 → 已进 G4；FaustBot `score_patch` 不适用 asyncio → 已进 G4 明确不引入；livekit/agents 作语音参考 → 已进 8.3。

### 8.5 全量核实结果（2026-09-19，逐条对源码核实）

> 初版报告的 oc-pet「缺口」多条来自 07 文档的**推演对齐表**、未逐条验码。以下为核实后判定（附证据）。**教训：推演结论实现前必须先读源码核实。**

| 编号 | 初判 | 核实 | 证据 |
|---|---|---|---|
| G1 VRM/3D 占位 | 缺口 | ✅**已落地（2026-09-19）** | 重写 `avatar/vrm_renderer.py`：QWebEngineView + three.js 0.180 + three-vrm 3.5.5；真渲染 VRM1.0 样片并回传 canvas 截图（`tools/verify_vrm_view.py`） |
| G2 A2A/委派 | 缺口 | ✅真 | core/ 无 a2a/delegate 能力 |
| G3 具身 | 缺口 | ❌**名称缺、机制在** | 全库确实无 "embody" 字样；但具身机制已在：MotionMixer 分层仲裁 + 表情/参数过期 + VA 连续坐标（`_va_target`）+ 口型时间线。又是「看名字不看机制」 |
| G4 记忆 图/分层/加权 | 缺口 | ⚠️**部分/已更正** | 分层**已在**（CompanionMemory 短期 + FactStore 状态机）；加权**已补**（score_patch, 09-19）；真缺仅：实体图、持久化向量索引 |
| G5 视觉不进对话/429 | 缺口 | ✅真 | depth04 |
| G6 [do:] 幻觉 | 缺口 | ❌**不成立** | 白名单+别名+安全回退已在（live2d_renderer.py:245/273、harness_adapter.py:1149） |
| G7 插件仅 1 | 缺口 | ❌**非缺口** | 插件机制完备：`tool_registry` + `conversation_engine._plugin_roots()` 扫**两个根**（`~/.hanako/plugins` + 本地 `plugins/`）、manifest 目录戳**热重载**、工具进 LLM 工具表、`ui/plugin_panel.py` 面板、`PetPermissions.tool_call_enabled` 闸门；本地只装 1 个（linjian-peek，8 个手机工具）是**数量**不是「机制缺失」 |
| O2 MCP 零安全 | 缺口 | ❌**不成立** | localhost+默认关+auth_token+allow_actions+白名单 |
| O3 口型参数/首帧 | 缺口 | ⚠️**部分存在** | 已有：`tts_provider/audio_timings.py` `_FRAME_MS=20` / `_MIN_SEG_MS=70` / `_MERGE_GAP_MS=60`（20ms帧/60ms闭嘴）；未落地：40ms起音/90ms收音/≤55%开口；首帧最低开口度对 oc-pet **不适用**（`_char_shape` 首帧≥0.30，`CLOSING_INITIALS` 从未被调用） |
| O5 pet-context 进 text | 缺口 | ✅真（但「无内容不拼块」已有） | harness_adapter.py:809 |
| O6 vision 共用 | 配置 | ❌**非代码缺口** | 配置链早已存在（env_config.py:401+，含 2 个测试）；只差用户选模型 |
| O7 表演队列 | 缺口 | ❌**非缺口** | `avatar/motion_mixer.py`：`Layer` 优先级 + `MotionRequest` + 3s 防闪烁 + 过渡缓动；已替代 5 道互斥补丁 |
| O8 pet.py _init_ 未搬 | 债 | ⚠️**实为 7 个** | pet.py（多了 _init_companion_memory） |
| O10 FrameBaker TODO | 债 | ❌**非缺口**（死代码） | ui/framebaker.py:137 `FrameBakerMCP` **零调用**；本机无 FrameBaker → 已改为 `NotImplementedError` 诚实报错 |
| A1 Web 管理界面 | 新增 | ✅**已落地（2026-09-19）** | `/pet/memory`（检视 + `?q=` 过滤）+ `/pet/memory/recall`（混合召回可视化）+ 浏览器检视页（`Accept: text/html`） |
| A3 表演时间线 | 新增 | ❌**非缺口** | 五轨齐备：口型时间线（live2d_renderer.py:3076）+ MotionMixer 分层仲裁 + 参数意图 TTL（`_expire_intent_params`）+ 表情过期（`_expire_expression_if_stale`） |
| A5 Host 持久化 | 新增 | ❌**非缺口** | 权限层在（`perception/flags.py` `PetPermissions`：screenshot/diary/session_read/cross_session/tool_call + active_hours，逐模块强制）；持久化分层在（`~/.oc-pet/` 自有 memory/usage/backups + `~/.hanako/` Hana 侧）+ `core/backup_service.py` **备份服务**。仅「artifact」作为命名概念未单列 |

**结论（2026-09-19 三轮 re-audit · 全部条目已验完）**：初版 14 条缺口/优化——**九条不成立**：G3/A3/O7 表演层（MotionMixer）、G4②分层、G6、G7 插件机制、O2、O6、O10（死代码）、A5 权限/持久化；**一条部分存在**：O3；**五条已落地**：O1（P0 429 退避 + P1 dHash 去重）、O4（API embedding）、O9（ishiki）、A1（记忆检视/召回可视化）、G4①（score_patch 加权）。

**真缺口·只剩三条**：① **G2/A4 A2A 委派**（core/ 无 a2a/delegate；重，且大概率不属 oc-pet 职责）；② **G5/O5 视觉进对话**（pet-context 塞进 text，WS 无 context 通道；**需 Hana 协议侧协同**）；③ **G4③ 实体图**（facts 有 `subject/predicate/object` 字段却无图结构；价值待定）。
（G1 已于 2026-09-19 落地：真 3D/VRM 渲染。）

**本报告的可靠性自评**：初版 14 条里 **9 条被逐条复核推翻**。根因固定：判据来自 07 推演表 + 「只看某个文件的 schema / 只搜关键词」，未验「有无真实调用方 / 机制是否以别的名字存在于别处」。**用前先看本节。**

**交叉验证**：独立只读 subagent 对 14 条清单逐条核实，与本节一致；细化：O3 为「部分存在」（20ms帧/60ms闭嘴已落地）、G4①为「部分存在」（有基础 RRF、无 score_patch）。

---

*作者：奥菲莉娅*
*落盘：W:/Games/Hanako/Work/projects/oc-pet/docs/开源对比-缺口与规划-2026-09-19.md*
