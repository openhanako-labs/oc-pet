# 核心
PySide6>=6.5.0
PySide6-Addons>=6.5.0
requests>=2.28.0
Pillow>=9.0.0
PyYAML>=6.0

# 音频（录音 + 播放）
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
soundfile>=0.12.0

# TTS（CosyVoice 间接依赖）
tqdm>=4.65.0

# ASR（可选，不装也不会崩）
# openai-whisper>=20230918
