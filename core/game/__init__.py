"""陪玩方向（Playmate）—— 游戏语境感知。

模块划分：
- ``registry``：游戏白名单 + 识别（纯函数，好测）
- ``session`` ：游戏会话状态机（启动 / 切后台 / 回前台 / 退出）

设计基线见 ``docs/playmate-direction-prd.md``（P0-1 游戏窗口识别）。
边界（PRD §5.1 红线）：**只读窗口标题/进程名**，不读内存、不注入进程、不做任何自动操作。
"""
